import styles from "./Notes.module.css";

interface Props {}

const Notes = ({}: Props) => {
  return (
    <div className={styles.container}>
      <h1>Notes</h1>
    </div>
  );
};

export default Notes;
